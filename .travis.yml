language: go

go:
  - 1.0
  - 1.1
  - 1.2
  - tip

install: make get-deps

before_install:
  - mv $HOME/gopath/src/github.com/typecho-app-store/typecho-app-store $HOME/src/
  - cd $HOME/src/typecho-app-store